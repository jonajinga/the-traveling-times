---
layout: layouts/author.njk
pagination:
  data: authors
  size: 1
  alias: authorSlug
  resolve: keys
permalink: "/author/{{ authorSlug }}/"
eleventyComputed:
  title: "{{ authors[authorSlug].name }}"
  bio: "{{ authors[authorSlug].bio }}"
  role: "{{ authors[authorSlug].role }}"
---
